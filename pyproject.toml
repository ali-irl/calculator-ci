[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-calculator"
version = "0.1.0"
description = "A basic calculator package with CI/CD"
readme = "README.md"
authors = [{ name = "Ali" }]
dependencies = []